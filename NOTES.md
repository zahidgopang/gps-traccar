Notes:
- This ZIP intentionally omits composer.json and vendor to avoid replacing core Laravel.
- After copying files into a fresh Laravel project, run:
  composer require laravel/breeze --dev
  php artisan breeze:install bootstrap
  composer require laravel/sanctum
  npm install
  npm run dev
